<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.banggusukmoviereview">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BangGuSukMovieReview"
        android:usesCleartextTraffic="true">
        <activity android:name=".WishListUpdateActivity"></activity>
        <activity android:name=".WishListWriteActivity" />
        <activity android:name=".WishListActivity" />
        <activity android:name=".SearchMovieActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".BoxOfficeActivity" />
        <activity android:name=".ReviewUpdateActivity" />
        <activity android:name=".ReviewResultActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ReviewWriteActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>